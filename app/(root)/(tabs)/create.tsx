import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function CreatePropertyScreen() {
    return (
        <SafeAreaView>
            <View>
                <Text>CreatePropertyScreen</Text>
            </View>
        </SafeAreaView>
    );
}
